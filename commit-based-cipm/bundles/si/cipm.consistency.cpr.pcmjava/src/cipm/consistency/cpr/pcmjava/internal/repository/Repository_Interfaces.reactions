import cipm.consistency.cpr.pcmjava.JavaModelAccess

import "http://www.emftext.org/java" as java
import "http://palladiosimulator.org/PalladioComponentModel/5.2" as pcm

reactions: repositoryInterfacesExistential
in reaction to changes in pcm
execute actions in java

reaction AddedInterface {
	after element pcm::OperationInterface inserted in pcm::Repository[interfaces__Repository]
	call {
		// TODO Locate Java correspondent or add a Java stub
	}
}

reaction RemovedInterface {
	after element pcm::OperationInterface removed from pcm::Repository[interfaces__Repository]
	call {
		// TODO Ask if Java correspondent should be removed
		// Check for dependencies and remove if not referenced
	}
}

routine createOrAddOrLocateJavaInterface(pcm::OperationInterface pcmIfc) {
	match {
		val javaIfc = retrieve optional java::Interface corresponding to pcmIfc
	}
	update {
		if (javaIfc.present) {
			// There is already a Java Interface corresponding to
			// the PCM Interface, nothing to do here
			return
		}
		
		// Assumption: Name of PCM and Java Interfaces must match
		// Java namespaces are excluded here
		val javaIfcs = JavaModelAccess.findPotentialConcreteClassifiers(pcmIfc.entityName)
		
	}
}