/**
 * Contains the means to filter GIT diff patches to get the actual changes from
 * them. Currently only consists of {@link DiffFilter} and its unit test.
 */
package cipm.consistency.fitests.repositorytests.util.difffilter;